define([
  "app",
  "api"
],

function (app, FauxtonAPI) {
  var Resources = {};

  // Resources.Hello = FauxtonAPI.View.extend({
  //   template: "addons/revisionTree/templates/docRetrieve"
  // });

  return Resources;
});